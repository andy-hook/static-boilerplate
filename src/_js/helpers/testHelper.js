'use scrict';

/* Test helper function
   ========================================================================== */

function testHelper(string) {
  return console.log(`Hello world! ${string} reporting in`);
}

module.exports = testHelper;